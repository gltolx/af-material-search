# af-material-search 架构设计文档(初稿 v0.1)

> 状态:**初步沉淀**,待"试跑实际流程"后根据真实问题迭代调整。
> 日期:2026-06-05

---

## 0. 一句话

给口播视频批量找**贴切的 B-roll 素材**:输入「一个选题下的一批口播稿」→ 自动到 **抖音 / B站 / 小红书** 搜索 → 在**检索结果页**人工预览 + 批量勾选 → 下载 → 入库到「该选题对应的素材知识库」。

---

## 1. 背景与目标

- 用户做**口播(真人出镜讲解)视频**,需要在口播里插入贴切的 B-roll 空镜/画面做**混剪**,提升丰富度与质量。
- 规模是**内容工厂级**:每天可能有多个选题,**每个选题 = 60 个稿子 → 60 个视频**,每个视频混剪多条素材(以视频为主、图片为辅)。
- 目标关键词:更丰富、更多、更广、更贴切;且**可规模化、低维护**。

---

## 2. 核心需求(用户明确要求)

1. 素材来源 = **直接从平台搜集**,不用任何传统素材库(Pexels/Pixabay/视觉中国等一律不要)。
2. 平台:抖音、B站、小红书、Google、YouTube;但题材**几乎全国内** → **主力 = 抖音 / B站 / 小红书**,Google/YouTube 仅偶尔补充(国外事件/人物)。
3. 匹配锚点 = **固定的口播稿**(每个视频单独对应一个稿子)。
4. **一个选题 = 60 个稿子**(每天可能多个选题);**这一批稿子对应同一个素材知识库**。
5. 每个视频混剪**多条**素材,**以视频为主**,也要图片。
6. **铁律:每个视频的素材尽量不重复**(平台会查重 / 限流)。
7. 素材库(知识库)**已由团队实现**,有上传/下载接口,**本系统只负责对接**,不自建存储。
8. 题材不限,但**必须禁止:黄赌毒、政治敏感**。
9. 发布以国内平台为主;素材源不限国内外;**版权由用户在发布切片时自行标注处理**,不构成取材阻碍。
10. **代码全部由 Claude 开发**;用户技术团队只做**轻维护**(cookie 跟版、排障)。
11. 低代码/零代码若能达标更好——但已确认:**纯零代码达不到此规模要求**,需自动化流水线(复用成熟开源,不重造轮子)。
12. 运行:**先本地(Mac)验证**,后迁**国内服务器**。
13. GPU:不确定 →(因下文砍掉精排)**本期不需要 GPU**。

---

## 3. 关键约束与铁律

| # | 铁律 | 含义 |
|---|---|---|
| 1 | 素材不重复 | 每个视频用的素材尽量互不相同,防平台查重/限流 |
| 2 | 合规红线 | 黄赌毒、政治敏感一律不取、不入库 |
| 3 | 全国内为主 | 抖音/B站/小红书 是主力源 |
| 4 | 选题=同一知识库 | 一个选题的 60 个稿子,素材都进同一个 KB |
| 5 | 只对接不自建库 | 知识库团队已建,本系统只调它的上传/下载接口 |
| 6 | 代码全包+低维护 | Claude 写全部代码;技术只跟 cookie/排障 |

---

## 4. 范围边界(本期做 / 暂不做)

**本期做(目标流水线):**
① 接稿 → ② 出检索词 → ③ 搜 3 平台(全部结果) → ④ 检索结果页(预览 + 批量勾选) → ⑤ 下载所选 → ⑥ 入库对接

**明确砍掉 / 延后(本期不做):**
- ❌ **去重**(暂不做,先把素材搜出来,后补)
- ❌ **贴切精排(CLIP/VL)**(贴不贴切由人在结果页判断;因此也**不需要 GPU**)
- ❌ **机审**(黄赌毒/政治自动过滤)(由人在结果页审)
- ❌ **独立的「下载后人审台」**(因结果页已能看视频预览,人工一道即可,等于"选 + 审"合并)
- ❌ **剪辑清单/时间轴产出**(流水线到入库为止)
- ❌ **per-video 素材分配**(默认由下游从 KB 里挑;除非用户要求加回)

**试跑优先聚焦:** **①→⑤(先搜出来 + 能下载)**;⑥ 入库待知识库接口到位后接。

---

## 5. 最终流水线

```
一个选题(一批稿子, 对应同一个素材知识库)
   │
 ① 接稿          读入这一批稿子(每稿 = 一个未来视频)
   │
 ② 出检索词       稿 → 搜索关键词(先朴素提取;后接 LLM 分镜增强)
   │
 ③ 搜 3 平台      抖音/B站/小红书,每个平台的搜索结果【全部拉出】(只取封面+元数据+可预览地址,先不下正片)
   │
 ④ 检索结果页 ★【唯一人工环节】
        每条稿 × 每个平台,结果全铺 → 看视频预览 → 批量勾选要的
   │
 ⑤ 下载所选       只下被勾选的(无水印源)
   │
 ⑥ 入库对接       过审素材 + 元数据 → 推进「该选题对应的素材知识库」(流水线终点)

   [后补插槽] 去重(④或⑤后)、LLM 分镜增强(②)、机审、规模化(队列/熔断/多账号cookie池/部署)
```

| 步骤 | 输入 | 输出 | 关键说明 |
|---|---|---|---|
| ① 接稿 | 一批稿子原文 | Script 列表 | 来源待定(文件夹 .txt / 接口 / 直接粘贴) |
| ② 出检索词 | 稿文本 | 每稿若干 query | v0 朴素关键词;后接 LLM 分镜出多角度检索词 |
| ③ 搜 3 平台 | query | 候选素材列表(含预览) | 每平台**全部结果**;需登录 cookie |
| ④ 结果页 | 候选列表 | 勾选集合 | 唯一人工:预览+批量勾选(详见 §6) |
| ⑤ 下载 | 勾选集合 | 本地素材文件 | 无水印源;断点续传 |
| ⑥ 入库 | 下载文件+元数据 | 进 KB | 待知识库接口 |

---

## 6. 检索结果页(唯一人工环节)——重点

这是本期的核心交互,把"选素材 + 合规审核"合并到这一页。

**布局**
- 按 **稿件** 分区;每个稿件下按 **平台(抖音 / B站 / 小红书)** 分列/分组。
- 每个平台把**该稿对应的所有搜索结果**铺出来(不截断)。
- 每个候选 = 卡片:**视频预览** + 标题 + 作者 + 时长 + 点赞/播放 + 原链接。

**预览**
- 抖音/小红书:用搜索结果返回的播放地址内联 `<video>` 预览。
- B站:`player.bilibili.com` iframe 内联预览。
- 兜底:封面图 + "看原片"跳转原链接。

**操作**
- 卡片 **批量勾选**(支持全选/按平台选/反选)。
- 「**下载所选**」一键进入 ⑤ 下载。

**状态机(队列驱动)**
`已搜到 → 待人工筛选(结果页) → 下载中 → 已下载 →(后续)入库`

---

## 7. 数据模型(初版)

```
Topic(选题): topic_id, name, kb_id(对应的素材知识库)
Script(稿件): script_id, topic_id, text(原文)
Candidate(候选素材):
    platform        # douyin | bilibili | xiaohongshu
    script_id
    query           # 由哪个检索词搜到
    candidate_id    # 唯一
    title, author
    cover_url       # 封面图
    preview_url     # 可内联播放地址(或空)
    page_url        # 原始链接
    duration        # 秒
    stats           # 点赞/播放等
    selected        # 是否被勾选
    # 后续字段: local_path, downloaded_at, review_status, source_meta(供版权标注)...
```

---

## 8. 技术选型(初定,可调)

| 模块 | 选型 | 备注 |
|---|---|---|
| 搜索引擎 | **MediaCrawler**(抖音/小红书/B站 关键词搜索) | 扛反爬、返回封面+播放地址;需登录 |
| 下载 | douyin-downloader / BBDown / XHS-Downloader / yt-dlp | 无水印源、断点续传 |
| Web 结果页 | FastAPI(本地起服务) | 单机即可 |
| 出检索词 | 先朴素关键词;后接 LLM(DeepSeek/Qwen) | LLM 分镜为增强项 |
| GPU | **本期不需要** | 已砍精排 |

> 设计原则:**少维护**(工具锁版本 + 每平台健康自检/失效告警 + 失效隔离),技术只需跟 cookie、看告警。

---

## 9. 运行环境与前置条件

**本机探测(2026-06-05,Bash 环境):**
- Python `3.9.6`(偏旧,建议 3.10+,待定)
- pip `21.2.4`
- `ffmpeg`:**缺失**(需安装,下载/转码要用)
- `yt-dlp`:**缺失**(需安装)
- `git` ✓ / `node` `v26.1.0` ✓

**网络出口(2026-06-05 实测修正):**
- 当前 Bash 出口 IP = 韩国 Incheon(`210.92.144.201`,LG DACOM)。
- ✅ 实测:**B站 搜索 API 从该 IP、无 cookie 直接可用**(返回 `code:0`,命中千条);**YouTube 经 yt-dlp 可搜**(yt-dlp 已装,版本 2025.10.14)。
- ⏳ **抖音 / 小红书** 搜索接口需 X-Bogus/x-s 签名 + 登录 cookie(可能还需中国大陆 IP)→ 必须用 MediaCrawler + 扫码登录才能测,是当前主要待通项。
- ⏳ **下载链路尚未验证**;两个已知坎:`ffmpeg` 未装(B站 DASH 合流要用)、B站取流/播放 API 可能比搜索更严(地域/登录)。

**登录:** 抖音/小红书 必须登录 cookie 才能搜到结果;B站 登录才有高清。

---

## 10. 待确认 / 待提供

1. **一批真实稿件原文**(用户即将提供 → 用于试跑)。
2. **中国大陆 IP / 代理 方案**(否则搜不动,见 §9)。
3. **三平台登录**(扫码存 cookie)。
4. **知识库上传/下载接口**(REST/SDK?鉴权?支持哪些元数据字段?)——⑥ 入库要用。
5. **稿件来源形式**(文件夹 .txt / 接口 / 直接粘贴)。
6. **per-video 素材分配** 是否要本系统做(默认不做,下游从 KB 挑)。

---

## 11. 关键决策日志(为什么这么砍)

| 决策 | 原因 |
|---|---|
| 入库为流水线终点,不做剪辑产出 | 知识库团队已建,本系统只对接;混剪由下游做 |
| 一个选题的一批稿 → 同一个 KB | 用户明确 |
| 砍 贴切精排(CLIP/VL)、不需要 GPU | 贴切由人在结果页判断 |
| 砍 机审 + 砍 独立人审台 | 结果页能看视频预览,人工一道即可("选+审"合并) |
| 砍 去重(暂时) | 先把素材搜出来,去重后补 |
| 每视频素材不重复 → 下游从 KB 挑 | 本系统保证 KB 够量;去重后续补 |
| 先本地验证,后国内服务器 | 用户要求,且便于试跑迭代 |

---

## 12. 后续迭代路线

试跑 **①→⑤** → 按真实问题修(网络/登录/搜索质量/预览/下载) → 接 **⑥ 入库** → 视情况补:**去重 → LLM 分镜增强 → 机审 → 规模化**(任务队列 / 熔断 / 多账号 cookie 池 / 部署国内服务器)。

## 13. 试跑发现与调整(2026-06-05 · 第一轮,选题:米卢2002世界杯)

**用了什么试:** 1 个选题、**20 条**人设稿(不是 60;数量待确认),内容高度同质:2002 世界杯回忆 + 米卢 + 奥克斯空调广告。

**实测结果:**
- ✅ B站搜索(KR IP / 无 cookie)可用;YouTube(yt-dlp)可用。
- ✅ **事件/人物/赛事** 检索极准:B站「2002世界杯 中国队 集锦」全是对口集锦/出线/五里河;挖到原片《国足的神 米卢奥克斯空调广告 2001.12.20》(`BV19gpjeSEJ5`)。YouTube 出《足球之夜·中国男足2002世界杯之旅》纪录片等。
- ❌ **怀旧空镜/氛围** 检索翻车:B站「老式电视机 怀旧」「夏天 风扇 空镜」返回测评/DIY/教程/液压机碾电视/手工,**无可剪空镜**(B站是内容/解说平台,纯空镜少且被测评教程占据)。
- ⚠️ 即便最优查询也有噪声(FIFA 游戏周报、五粮液广告混入)。

**据此调整(已纳入架构):**
1. **平台分工(写死进检索词路由):** 事件/人物/赛事/纪录片切片 → **B站 + YouTube**;怀旧空镜/氛围/生活场景/产品镜头 → **抖音 + 小红书**。不分工就出垃圾(已被实测证明)。
2. **LLM 分镜由"增强项"升为"必需":** 朴素关键词对事件类够用,对空镜/抽象类(如"一群人挤在一起的温度")必翻车;需 LLM 把抽象句翻成「具体可拍画面 + 平台适配词 + 去哪个平台」。
3. **加平台侧过滤降噪:** B站用分区(体育)、时长(<5min 取切片 / 标记长视频待切)、按播放排序。
4. **长视频切片(后补):** 纪录片/集锦/谈话(40+min)需按字幕/场景/`--download-sections` 切片,非本期重点,先标记。
5. **前置筛选页价值被实证:** 最优查询仍混噪声 → 人在结果页一眼剔除,这道人工环节省不掉且高效。
6. **优先接通 抖音/小红书:** 它们是"怀旧空镜"的关键源,而本选题恰有一半是怀旧氛围。需 MediaCrawler + 扫码登录。

**待办(下一轮试跑):** 接通抖音/小红书搜索 → 验证下载链路(装 ffmpeg;实测 B站/YT 下片) → 把结果铺成「前置筛选页」。

---

## 14. 查询生成规则（修复"搜索姿势"·2026-06-05）

**复盘:**「90年代 客厅 看电视」搜出来全是现代客厅/电视,不沾 90 年代。根因不是搜不到,是查询姿势错。

**根因(本质):描述 ≠ 召回。**
1. **没消歧:**「90年代」是弱词,会被「客厅 看电视」带偏到现代内容;需加「上世纪」这种无歧义**年代锚点**把意图钉死。
2. **没说平台的话:** 怀旧素材是用「怀旧/回忆杀/那些年/老物件/年代感/老式」被标题打标的;不能用中性场景白描。
3. 我把口播稿**字面翻译**成搜索词(优化"像不像那画面"),而非按**检索意图+平台行话+看结果回修**来搜(优化"搜不搜得到那条")。

**统一修法(对所有查询生效,非单条补丁):**
- **必带意图锚点:** 年代/情绪/风格类查询强制带无歧义锚点 —— 时代(上世纪/八九十年代/90年代初)、属性(老式/复古/怀旧/年代感)、情绪(回忆杀/那些年/童年记忆)。**禁止裸投弱词**(如单独"90年代")。
- **说平台的话:** 按创作者实际标题/标签拼(怀旧/回忆杀/老物件/实拍/空镜/素材),而非场景白描。
- **一概念多变体:** 每个画面概念产出 3-5 条带不同锚点/同义词的查询保覆盖。
- **看结果回修(迭代):** 搜完判断是否跑偏(现代/串题材),偏了**自动加更强锚点重搜**,不一次性交付弱查询。

**落地:** 写进 LLM 分镜→检索词 的 prompt 作硬约束,每条查询出厂前过一遍"锚点检查"。

---

> 本文件随试跑迭代更新。
