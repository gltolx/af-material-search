# 素材关联度打分与筛选规则 v1

> 目的:关键词搜不准是必然(平台不保证),所以**对每条候选按"与文稿的关联度"打分,低于阈值淘汰**,优胜劣汰;不够量就扩/换关键词再搜,直到**量 + 质**都够。
> **核心:标题为主做匹配,首帧图作参考。** 标题能过滤掉大部分不相关素材。

## 输入

- **视觉概念集**:从口播稿提炼(每个选题一份 `relevance_spec.json`),含每个概念的「概念词 + 权重」、全局「负面词」「可剪性词」。这份 spec 的"语义智能"由 Claude 读稿子产出(每选题一次)。
- **候选**:`{platform, id, title, cover(本地封面), url, query}`。

## 打分(0–100,标题驱动)

1. **硬负面词 → 直接 0 分淘汰**:液压机/碾/拆解/拆机/测评/评测/开箱/教程/教学/怎么/如何/带货/优惠/预售/小说/电视剧/游戏/手游/攻略/表情包… (跑题或不可剪)。
2. **概念命中累加**:标题命中某概念的任一概念词 → 加该概念权重(单概念按其 weight,25–40)。命中多个概念分更高。
3. **可剪性微调**:标题含 集锦/现场/回顾/实拍/空镜/素材/纪录/4K/高清/混剪/名场面 → +10;含 解说/口播/盘点/争论/点评/分析(是"讲"不是"画面")→ −12。
4. 归一化到 0–100。

## 首帧图(辅助,仅边界用)

- 只对**边界分(默认 50–65)**的候选,Claude 去看本地缓存封面 `covers/{id}.jpg` 确认/微调:真有所需画面(比赛/球迷/老电视/米卢)→ 上调保留;纯文字/牛皮癣广告/与标题不符 → 下调淘汰。
- 标题信息过少时也用封面补判。
- **不对全部候选逐张看图**(几百条不现实)——封面只做"标题定不下来时"的裁判。

## 阈值与产出

- 默认阈值 **60**(可调):≥60 保留,<60 淘汰。
- 保留集按分**降序**;每条带 `score / 命中概念 / flags / 理由`。
- 报告:总数、各平台保留数、边界待看图清单。

## 闭环(达量又达质)

```
搜(§14 多锚点) → 打分 → 阈值过滤(优胜劣汰)
  ↓ 保留数 < 目标量?
  是 → 扩散/换关键词(同义/上下位/新角度变体)→ 再搜 → 再打分
  否 → 完成(量+质都够)
```

## 实现

- `score_candidates.py`:读 `candidates.json` + `relevance_spec.json` → 输出 `scored.json`(含 score/keep/hits/flags)+ 摘要。纯规则、无 API、可复用、确定性。
- 语义靠"概念词表塞足口语变体"兜(如怀旧看球塞:看球/球迷/欢呼/围着电视/那年/那些年/挤/小卖部…)。
- 阈值、权重、词表都在 spec 里,按实际效果回调(跟 §14 一样,单一事实源)。
